# 마을 안전 사각지대 예측 지도

## 설치 및 실행
```bash
npm install
npm run dev
```

## Mapbox 토큰 설정
.env.local 파일에 실제 Mapbox 토큰을 입력하세요.

## Vercel 배포
```bash
npx vercel
```
Environment Variables에 NEXT_PUBLIC_MAPBOX_TOKEN을 설정하세요.
